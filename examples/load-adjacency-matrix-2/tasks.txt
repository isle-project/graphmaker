# This adjacency matrix example currently only works correctly with GPT-4.
# However, the use of prefix and suffix with the :exec command works in general.
# You need to use '', "", or `` as delimiters on the prefix and suffix if the strings
# have spaces in them.

:exec prefix='Assume that the following is an adjacency matrix for an undirected graph on nodes A-E in order' ./examples/load-adjacency-matrix-2/matrix.txt
